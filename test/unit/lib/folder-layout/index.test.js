import { test, expect } from '@playwright/test';


test.describe('folder-layout', () => {

  test.describe('presentation with only a front slide', () => {

    const index = {
      slides: [{ id: 'front' }]
    };

    test('x', () => {

      //      locateSection(index, )

    });

  });

});
